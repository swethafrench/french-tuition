json
{
  "builds": [
    { "src": "api/*.py", "use": "@vercel/python" },
    { "src": "next.config.ts", "use": "@vercel/next" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1.py" },
    { "src": "/(.*)", "dest": "/$1" }
  ]
}